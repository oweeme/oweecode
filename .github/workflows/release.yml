name: Release

on:
  push:
    tags:
      - 'v*'

# Permiso necesario para crear GitHub Releases
permissions:
  contents: write

jobs:
  release:
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-22.04
            bundles: deb,appimage
          - os: windows-latest
            bundles: nsis,msi
          - os: macos-latest
            bundles: dmg

    runs-on: ${{ matrix.os }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: oweecode/package-lock.json

      - name: Setup Rust stable
        uses: dtolnay/rust-toolchain@stable

      - name: Rust cache
        uses: swatinem/rust-cache@v2
        with:
          workspaces: oweecode/src-tauri -> target

      - name: Install Linux system dependencies
        if: matrix.os == 'ubuntu-22.04'
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libwebkit2gtk-4.1-dev \
            libappindicator3-dev \
            librsvg2-dev \
            patchelf \
            libssl-dev \
            pkg-config \
            libssh2-1-dev \
            fuse \
            libfuse2

      - name: Install frontend dependencies
        working-directory: oweecode
        run: npm ci

      - name: Build and release
        uses: tauri-apps/tauri-action@v0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          TAURI_SIGNING_PRIVATE_KEY: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY }}
          TAURI_SIGNING_PRIVATE_KEY_PASSWORD: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY_PASSWORD }}
        with:
          projectPath: oweecode
          tagName: ${{ github.ref_name }}
          releaseName: OweeCode ${{ github.ref_name }}
          releaseBody: |
            ## OweeCode ${{ github.ref_name }}

            IDE de escritorio construido con Tauri 2 + Vue 3 + Rust.
            Desarrollado por Héctor Martínez — hector@oweeme.com — oweeme.com

            ### Instaladores disponibles
            - **Linux**: `.deb` (Debian/Ubuntu/Neon) y `.AppImage` (universal)
            - **Windows**: `.exe` (NSIS installer) y `.msi`
            - **macOS**: `.dmg`

            ### Instalación rápida
            **Linux (deb):**
            ```bash
            sudo dpkg -i OweeCode_*.deb
            ```
            **Linux (AppImage):**
            ```bash
            chmod +x OweeCode_*.AppImage && ./OweeCode_*.AppImage
            ```
            **Windows:** Ejecutar el `.exe` o `.msi` como administrador.
            **macOS:** Abrir el `.dmg` y arrastrar OweeCode a Aplicaciones.
          releaseDraft: true
          prerelease: false
          args: --bundles ${{ matrix.bundles }}
